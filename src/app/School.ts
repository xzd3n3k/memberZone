export interface School {
  registration_number: string;
  name: string;
  principal: string;
  phone: string;
  address: string;
  province: string;
  email: string;
  active: boolean;
  payed: boolean;
}
