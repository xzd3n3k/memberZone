export interface School {
  id?: number;
  registration_number: string;
  name: string;
  principal: string;
  phone: string;
  street: string;
  post_code: string;
  zip_code: string;
  city: string;
  country: string;
  province: string;
  email: string;
  active: boolean;
  payed: boolean;
}
